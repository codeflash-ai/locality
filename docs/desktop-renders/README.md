# Desktop UI Renders

Open `index.html` directly in a browser or serve this directory locally:

```sh
python3 -m http.server 5174 --directory docs/desktop-renders
```

The render board is a static design artifact. It is not the Tauri implementation.
