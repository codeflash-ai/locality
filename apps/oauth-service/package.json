{
  "name": "@codeflash-ai/locality-auth-broker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal OAuth broker for Locality connector auth.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run typecheck && npm test"
  },
  "dependencies": {
    "hono": "^4.12.31"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260721.1",
    "typescript": "^5.9.0",
    "vitest": "^4.1.9",
    "wrangler": "^4.113.0"
  },
  "overrides": {
    "sharp": "^0.35.3"
  },
  "overrides": {
    "sharp": "0.35.3"
  },
  "engines": {
    "node": ">=22"
  }
}
