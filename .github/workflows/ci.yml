name: ci

on:
  push:
    branches:
      - main
    paths-ignore:
      - "docs/**"
      - "docs-site/**"
  pull_request:
    paths-ignore:
      - "docs/**"
      - "docs-site/**"

jobs:
  oauth-service:
    name: OAuth service tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/oauth-service/package-lock.json
      - name: Install OAuth service dependencies
        run: npm ci
        working-directory: apps/oauth-service
      - name: Check OAuth service
        run: npm run check
        working-directory: apps/oauth-service
      - name: Audit OAuth service dependencies
        run: npm audit
        working-directory: apps/oauth-service

  linux:
    name: Linux tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json
      - name: Install Linux desktop dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            fuse3 \
            libglib2.0-dev \
            libgtk-3-dev \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            expect \
            patchelf \
            sqlite3
      - name: Check Rust formatting
        run: cargo fmt --all -- --check
      - name: Validate CI workflow contracts
        run: tests/ci_workflow_contract.sh
      - name: Validate connector extension contracts
        run: |
          cargo test -p locality-connector --test manifest_contract --test conformance_testkit
          cargo test -p localityd --test connector_manifest
      - name: Run Rust tests
        run: cargo test --workspace --all-targets
      - name: Run Amika snapshot shell test
        run: make test-init-amika-locality-snapshot
      - name: Install desktop dependencies
        run: npm ci
        working-directory: apps/desktop
      - name: Run desktop web UI tests
        run: npm test -- --run
        working-directory: apps/desktop
      - name: Build desktop web UI
        run: npm run build
        working-directory: apps/desktop

  linux-fuse:
    name: Linux FUSE smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install FUSE dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y fuse3 libfuse3-dev pkg-config sqlite3
      - name: Verify hosted runner exposes FUSE
        run: |
          if [ ! -e /dev/fuse ]; then
            echo "::error::/dev/fuse is not present. Real online-only FUSE mount smoke is not covered by this hosted runner."
            exit 1
          fi
      - name: Run real Linux FUSE smoke test
        run: LOCALITY_FUSE_SMOKE=1 LOCALITY_FUSE_SMOKE_REQUIRED=1 tests/run_linux_fuse_ci.sh tests/linux_fuse_smoke.sh

  windows:
    name: Windows tests
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run Windows Rust tests
        run: >
          cargo test
          -p locality-core
          -p locality-connector
          -p locality-store
          -p locality-platform
          -p locality-notion
          -p localityd
          -p loc-cli
          -p locality-cloud-files
          --all-targets
          --no-fail-fast
      - name: Check Linux-only FUSE stub
        run: cargo check -p locality-fuse
      - name: Check Windows desktop shell
        run: cargo check -p locality-desktop

  macos:
    name: macOS tests
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json
      - name: Report macOS File Provider coverage
        run: |
          echo "::warning::This hosted macOS job compiles the File Provider code, but does not register a real File Provider domain. Real online-only File Provider smoke needs a signed app/extension and macOS user approval."
      - name: Run Rust tests
        run: cargo test --workspace --all-targets
      - name: Test macOS File Provider package
        run: swift test --package-path platform/macos/LocalityFileProvider
      - name: Install desktop dependencies
        run: npm ci
        working-directory: apps/desktop
      - name: Build desktop web UI
        run: npm run build
        working-directory: apps/desktop
